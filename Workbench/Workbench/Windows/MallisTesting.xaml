<Window xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        x:Class="Workbench.MallisTesting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Workbench" 
        xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
        mc:Ignorable="d"
        Title="MallisTesting" Height="450" Width="800" MouseMove="Window_MouseMove"
        Name="MallisTestWindow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <!-- Menu Bar -->
        <Menu Width="Auto" Grid.Column="0" Grid.Row="0" Name="FileEditMenu">
            <MenuItem Header="File">
                <MenuItem x:Name="AppExit" Header="Exit" HorizontalAlignment="Left" Width="auto"/>
            </MenuItem>
            <MenuItem Header="Tools">
                <MenuItem x:Name="Options" Header="Options" HorizontalAlignment="Left" Width="auto"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem x:Name="About" Header="About" HorizontalAlignment="Left" Width="auto"/>
            </MenuItem>
        </Menu>
        <!-- Mod view -->
        <Grid Grid.Row="1" Name="FileViewGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Column="0" Grid.Row="0" Header="Mod Files">
                <ScrollViewer>
                    <TreeView>
                        <TreeViewItem Header="Mod">
                            <TreeViewItem Header="meta.json"/>
                        </TreeViewItem>
                    </TreeView>
                </ScrollViewer>
            </GroupBox>
            <!-- Jet view -->
            <GroupBox Grid.Column="0" Grid.Row="1" Header="Jet Files" Height="Auto" Margin="0" >
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <TreeView Grid.Column="0" Grid.Row="2">
                        <TreeViewItem Header="Jet" Expanded="TreeViewItem_Expanded">
                            <Label Content="Test"/>
                        </TreeViewItem>
                    </TreeView>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
